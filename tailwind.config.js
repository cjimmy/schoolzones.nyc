import typography from '@tailwindcss/typography';

const config = {
  darkMode: 'class',
  content: [
    './app/**/*.{js,ts,jsx,tsx}',
  ],
  plugins: [
    typography,
  ],
};

export default config; 